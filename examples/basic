-- Examples: Uncomment one of these lines, then run `./Main`.

-- Π X : ★ → X                                 -- 0
-- Π X : ★ → Π x : X → X                       -- 1
-- λ X : ★ → λ x : X → x                       -- tt
-- Π X : ★ → Π x : X → Π x : X → X             -- 2
-- λ X : ★ → λ x : X → λ y : X → x             -- true
-- λ X : ★ → λ x : X → λ y : X → y             -- false
